# Ініціалізація змінних
current_sum = 0  # Змінна для зберігання поточної суми
number = 1       # Число, яке буде додаватися на кожній ітерації

print("--- Підсумовування чисел до перевищення 100 ---")

# Використовуємо нескінченний цикл (while True), щоб гарантовано 
# досягти умови зупинки всередині циклу
while True:
    
    # 1. Перевірка умови перед додаванням
    # Якщо поточна сума (current_sum) вже перевищує 100, 
    # ми зупиняємо цикл оператором 'break'
    if current_sum + number > 100:
        print(f"\nУмова перевищення 100 виконана.")
        print(f"При додаванні числа {number} сума ({current_sum} + {number}) перевищить 100.")
        break  # Вихід із циклу
        
    # 2. Виконання циклічних операцій (якщо 'break' не спрацював)
    current_sum += number  # Додаємо поточне число до суми
    print(f"Крок {number}: Додаємо {number}. Нова сума = {current_sum}")
    
    number += 1            # Переходимо до наступного числа
    
# 3. Виведення фінального результату
print("\n--- Результат ---")
print(f"Фінальна сума (до перевищення): **{current_sum}**")
print(f"Останнє додане число: **{number - 1}**") # number на 1 більше після останньої ітерації
